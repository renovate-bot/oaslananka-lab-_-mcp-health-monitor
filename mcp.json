{
  "name": "mcp-health-monitor",
  "version": "1.0.5",
  "mcpName": "io.github.oaslananka/mcp-health-monitor",
  "description": "Monitor your MCP servers: health checks, uptime, response times, and Azure DevOps pipeline status",
  "transport": "stdio",
  "command": "npx",
  "args": [
    "-y",
    "mcp-health-monitor"
  ],
  "env": {
    "HEALTH_MONITOR_DB": {
      "description": "Custom path for the SQLite database (optional)",
      "required": false,
      "default": "~/.mcp-health-monitor/health.db"
    },
    "HEALTH_MONITOR_AUTO_CHECK": {
      "description": "Set to '1' to enable background scheduled checks",
      "required": false,
      "default": "0"
    }
  },
  "tools": [
    "register_server",
    "check_server",
    "check_all",
    "get_uptime",
    "get_dashboard",
    "get_report",
    "list_servers",
    "unregister_server",
    "set_alert",
    "get_monitor_stats",
    "register_azure_pipelines",
    "check_pipeline_status",
    "get_pipeline_logs",
    "check_all_projects"
  ]
}